FloatLayout:
    Background:
        id: background  #   идетнитфикатор фона, к которуму обращаемся в on_start(self)
        canvas.before:
            Rectangle:
                size: self.size
                pos: self.pos
                source: 'sky.png'
            Rectangle:
                size: self.width, 300
                pos: self.pos[0], self.pos[1] + self.height - 300
                texture: self.cloud_texture